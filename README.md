# cryptanalysis

## How to run

Ensure you have dependencies

```uv sync```

Assuming you have ciphers in the cipher dir, you can run below:

```uv run main.py -c <CIPHER_NAME> -r <RESTARTS> -b <BATCH_SIZE>```
